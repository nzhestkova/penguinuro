import { SetStatusDirective } from "./set-status.directive";

describe("SetStatusDirective", () => {
  it("should create an instance", () => {
    const directive = new SetStatusDirective();
    expect(directive).toBeTruthy();
  });
});
